# Project MCP (optional). Prefer user ~/.grok absolute path.
# Grok only starts project-local servers when the folder is trusted.
[mcp_servers.evident-output]
command = "/Users/zbornheimer/.local/bin/evident-output-mcp"
enabled = true
startup_timeout_sec = 60
